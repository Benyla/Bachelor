# config.yaml

experiment:
  name: "cell_vae_experiment_full_data"
  neptune_project: "benyla/Bachelor"

training:
  epochs: 30
  batch_size: 32
  learning_rate: 0.001

model:
  in_channels: 3
  latent_dim: 150

data:
  dummy: False
